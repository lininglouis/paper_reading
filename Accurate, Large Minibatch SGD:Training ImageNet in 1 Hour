linear scalling rules:    batch_size increased from m -> n*m, then learning rate increased from lr  ->  n*lr  <br> 
* hyperparameter free search rules
* can apply to the case where the minibatch is as large as 8196.
